{/*import express from "express";
import cors from "cors";
import datosRoutes from "../routes/firms.routes.js";

const app = express();

app.use(cors());
app.use(express.json());

app.use("/api", datosRoutes);

export default app;*/}
